#!/usr/bin/env bash
set -euo pipefail

need() {
  command -v "$1" >/dev/null 2>&1 || { echo "NG: missing '$1'"; exit 1; }
  echo "OK: $1"
}

need git
need rsync
need task

echo "OK: all essentials installed"
